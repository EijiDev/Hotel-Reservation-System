# Force text files to use LF line endings
* text=auto eol=lf

# Treat PHP, HTML, CSS, JS as text
*.php text
*.html text
*.css text
*.js text
*.json text
*.xml text

# Prevent binary files from being diffed
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary

# Lock composer.lock to prevent accidental merge conflicts
composer.lock -merge

# Ensure consistent behavior for shell scripts
*.sh text eol=lf
